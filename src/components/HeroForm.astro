---
import SectionContainer from "./SectionContainer.astro"
---
<SectionContainer>
  <div class="relative rounded-2xl overflow-hidden">
    <!-- Fondo con brillo reducido -->
    <div class="absolute inset-0 bg-hero bg-no-repeat bg-cover brightness-80 z-0"></div>
  
    <!-- Contenido sobre el fondo -->
    <div class="relative z-10 text-amber-500 py-24 px-6 text-center">
      <h1 class="text-4xl sm:text-5xl md:text-6xl font-extrabold mb-6">
        Hacete parte de la aventura
      </h1>
      <p class="text-lg sm:text-xl max-w-2xl mx-auto mb-10 text-amber-500">
        Conocé junto a nosotros las más interesantes rutas que La Rioja, Argentina tiene para ofrecerte.
      </p>
      <div class="flex flex-col sm:flex-row justify-center items-center gap-4">
        <a href="#" class="bg-tertiary hover:opacity-60 text-accent font-medium py-3 px-6 rounded-lg text-base transition">
          Descubre los destinos
        </a>
        <a href="#" class="bg-transparent border border-gray-300 hover:border-gray-500 hover:opacity-60 text-gray-300 font-medium py-3 px-6 rounded-lg text-base transition flex items-center gap-2">
          Reservá tu boleto
          <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"></path>
          </svg>
        </a>
      </div>
    </div>
  </div>
  
</SectionContainer>

